# Improvements

`pending/` contains improvement suggestions produced by `npm run improvement:review`.

`applied/` contains owner-approved improvement records.

Review every pending suggestion before applying it. Colombo must not automatically change its own setup without owner approval.
